
# libtool requires some M4 scripts to be added to the source tree.  Make sure that
# Autoconf knows where to find them.
ACLOCAL_AMFLAGS = -I m4

lib_LIBRARIES = libtoolbox.a
SUBDIRS = str_manipulations utils voip
